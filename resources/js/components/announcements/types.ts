export interface Announcement {
    id: string;
    title: string; // üst kırmızı başlık
    subtitle: string; // alt büyük başlık
    excerpt: string; // kısa açıklama
    image: string; // logo / afiş
    date: string;
}
